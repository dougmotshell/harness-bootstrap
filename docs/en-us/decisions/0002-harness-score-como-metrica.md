# 0002 — Adopt harness-score as the harness metric

> **TODO: not translated yet.** The source of truth for this document is
> [`docs/pt-br/decisions/0002-harness-score-como-metrica.md`](../../pt-br/decisions/0002-harness-score-como-metrica.md). Translate it here;
> do not fork the content. The filename must stay identical across language
> subtrees, so a translation is always a sibling.

<!-- Use this stub for every file that exists in docs/pt-br/ but not yet in
     docs/en-us/ (or any other language subtree). An untranslated file gets this
     pointer, never a silently missing sibling: the parity check
     `diff <(cd docs/pt-br && find . -type f | sort) <(cd docs/en-us && find . -type f | sort)`
     must come back empty. -->
